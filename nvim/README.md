# Минимальный конфиг Neovim

Простая конфигурация Neovim с красивой темой Catppuccin Mocha и подсветкой синтаксиса. Идеально подходит для использования в качестве текстового редактора без лишних IDE-функций.

## 🚀 Быстрый старт

### Требования

- Neovim >= 0.8.0
- Git

### Установка

1. **Создайте резервную копию** существующей конфигурации (если есть):
   ```bash
   mv ~/.config/nvim ~/.config/nvim.backup
   ```

2. **Создайте директорию** для новой конфигурации:
   ```bash
   mkdir -p ~/.config/nvim
   ```

3. **Скопируйте конфигурацию** в файл `~/.config/nvim/init.lua`

4. **Запустите Neovim** - плагины установятся автоматически:
   ```bash
   nvim
   ```

### Первый запуск

При первом запуске произойдет автоматическая установка:
- Менеджера плагинов Lazy.nvim
- Цветовой схемы Catppuccin
- Treesitter для подсветки синтаксиса

Процесс займет несколько минут.

## 🎨 Возможности

### Внешний вид
- **Тема**: Catppuccin Mocha - современная темная тема
- **Номера строк**: Для удобной навигации
- **True colors**: Поддержка полной цветовой палитры

### Подсветка синтаксиса (Treesitter)
- Современная подсветка синтаксиса
- Поддержка языков: Bash, Python, Go, YAML, Markdown, Lua
- Автоматическая установка парсеров
- Умные отступы

### Управление мышью
- Полная поддержка мыши для навигации и выделения текста
- Прокрутка колесиком мыши
- Клик для установки курсора

Конфигурация использует стандартные команды Neovim без дополнительных горячих клавиш:

## 🔧 Добавление новых языков

Для добавления поддержки нового языка в Treesitter:

1. **Откройте конфигурацию** `~/.config/nvim/init.lua`

2. **Добавьте язык в список**:
   ```lua
   ensure_installed = { 'bash', 'python', 'go', 'yaml', 'markdown', 'lua', 'новый_язык' },
   ```

3. **Перезапустите Neovim** и выполните `:TSUpdate`

## 🐛 Решение проблем

### Плагины не устанавливаются
```bash
# Удалите кэш и переустановите
rm -rf ~/.local/share/nvim
nvim
```

### Проблемы с темой
```vim
:colorscheme catppuccin
```

### Treesitter не работает
```vim
:TSUpdate
```

Если проблемы продолжаются:
```bash
# Переустановите Treesitter
rm -rf ~/.local/share/nvim/lazy/nvim-treesitter
nvim
```

## 📁 Структура конфигурации

```
~/.config/nvim/
├── init.lua                 # Основной файл конфигурации
└── lazy-lock.json          # Автогенерируемый файл версий плагинов
```

### Важные директории данных
```
~/.local/share/nvim/
├── lazy/                   # Установленные плагины
└── lazy-lock.json          # Файл блокировки версий плагинов
```

## 🔄 Обновление

### Обновление плагинов
```vim
:Lazy update
```

### Обновление Treesitter парсеров
```vim
:TSUpdate
```
---

**Простой и эффективный текстовый редактор готов к работе! ✨**